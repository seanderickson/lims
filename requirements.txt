# requirements, where commented, show the last verified version
Django >=1.8,<1.9 #=1.8.5
Pillow #>=3.0.0,<3.1
SQLAlchemy >=1.0.9,<1.1
aldjemy #==0.6.0
argparse #1.2.1
# distribute>=0.6.24
django-tastypie >=0.12.2,<0.13
fake-factory==0.7.4
factory-boy==2.7.0
#factory-boy #2.6.0
#fake-factory #==0.5.3
docutils
# ipython
mimeparse #==0.1.3
prettytable >= 0.7
# NOTE: psycopg2 package is now a source package; compile requires
# libpq-dev and python-dev for header files
# psycopg2-binary requires no build
# NOTE(2): many sub packages require "psycopg2" at this time; so hold off on this update for now (20180628)
#psycopg2 >=2.6
psycopg2>=2.7,<2.8
python-dateutil #==2.4.2
pytz #==2015.7
# 20180330 - Peg the version to 2.8.1 for authenticate.med.harvard.edu errors like:
# Max retries exceeded with url: /wsAuthenticate.asp (Caused by SSLError(SSLError(8, '_ssl.c:507: EOF occurred in violation of protocol')
requests==2.8.1
certifi
six >=1.10
wsgiref >=0.1.2
xlrd #=0.9.4
xlwt >=1
XlsxWriter #==0.7.7
openpyxl #==2.3.4
unicodecsv #==0.14.1
ldap3 #==2.5
